# iOS (placeholder)

The Remote Control iOS app that used to live here has been **removed**. The Mac bridge uses only **WebDriverAgent (WDA)** on the iPhone — see **`../wda/`** and **`../bridge/README.md`**.

No iOS app in this repo is required for remote control.
